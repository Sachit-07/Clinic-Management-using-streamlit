# MediCare Clinic Management System

A simple Streamlit clinic management system with a staff login portal and dashboard.

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Main flow

Login Portal → Dashboard → Patients / Appointments / Doctors

The current records are sample data and can later be connected to a database.
